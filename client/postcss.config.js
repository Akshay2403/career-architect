// postcss.config.js
export default {
  plugins: {
    "@tailwindcss/postcss": {}, // Tailwind CSS v4 compatible plugin
    autoprefixer: {}, // Ensures CSS compatibility across different browsers
  },
};
